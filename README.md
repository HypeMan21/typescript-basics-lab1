# Лабораторна робота №1 з TypeScript

Репозиторій містить базову конфігурацію проєкту на TypeScript:
- налаштований `tsconfig.json`;
- приклад простого коду на TypeScript;
- компіляція у директорію `dist`.

Основна робоча гілка для розробки — `develop`, стабільна — `main`.
